/**
 * Barrel export para contexts
 *
 * Facilita los imports:
 *   import { MusicProvider, useMusicContext } from '../context';
 */

export { MusicProvider, useMusicContext } from './MusicContext';
export { default as MusicContext } from './MusicContext';
